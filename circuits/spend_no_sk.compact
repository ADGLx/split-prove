// RETIRED — kept as documentation only.
//
// This file was a stand-alone prototype of the server-side split spend
// circuit, used during the v1/v2 design. The actual server circuit now
// lives in the submodule at:
//
//   deps/midnight-ledger/zswap/zswap-split.compact
//
// See that file for the Solution A version (Phase 3 of the implementation
// plan dropped the `publicKey` and `coinCommitment` per-spend disclosures
// from the spend-split path). The compiled artifacts referenced by Rust
// code are under `deps/midnight-ledger/zswap/static/spend-split.*`.
//
// We leave this stub in the tree to preserve the historical reference path
// the docs and design notes link to. It is not compiled or used at runtime.
